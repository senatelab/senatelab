name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  announce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Echo tag
        run: echo "Tagged ${{ github.ref_name }}"
      - name: Done
        run: echo "release workflow ok"
